apply plugin: 'kotlin'

dependencies {
    rootProject.ext.domainDependencies.each {
        implementation it
    }

    rootProject.ext.domainTestDependencies.each {
        testImplementation it
    }
}
